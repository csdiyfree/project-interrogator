# Agent FE3 · v02 指南页导航补全

## 你的身份与任务
你是前端工程师,为「项目拷打器」v02 给**改进指南页**补上返回导航,使其融入新的页面互通(反馈#1)。指南页其余功能(红黄绿灯、todo 勾选、再次拷打、多轮历史切换)保持不变。

## 前置
**FE0 v02 已完成**(`ProjectDetail.resume_id` 已加;顶栏常驻「信息页/上传」入口)。

## 先读(在仓库内)
- `develop_prompt/v02/00_第二轮改动计划.md`(反馈#1)
- 现有代码:`frontend/src/pages/GuidePage.tsx`(已通过 `getInterrogation(id)`→`getProject(project_id)` 拿到 `project`,现在其含 `resume_id`)

## 铁律
- 只经 endpoints 取数。

## 改动清单
- 在指南页顶部(项目名一行附近)加一个克制的「返回」入口:
  - 「返回信息页」→ `navigate('/resume/' + project.resume_id)`(`project` 已有,加上 v02 的 `resume_id`)。
  - 可选再加「返回本轮拷问」→ `navigate('/interrogations/' + id)`(便于回看对话);保持图标化、不堆按钮。
- 顶栏已常驻「信息页/上传」入口(FE0 提供),本页只需补这条页面内的上下文返回即可,避免与顶栏重复堆砌。
- 不改:`getGuide` 轮询、todo 勾选、`再次拷打`、多轮历史切换器等既有逻辑与视觉。

## 验收(`VITE_USE_MOCK=true`,深链 `/interrogations/seed-it-ano-2/guide`)
- [ ] 指南页可一键返回该简历的信息页(`/resume/:resume_id`);可选的「返回拷问」可回到对话。
- [ ] 红黄绿灯、todo 勾选、再次拷打、多轮历史切换均不回归。
- [ ] 零文案、风格统一;仅经 endpoints 取数。

## 不要做
- 不改指南生成/评分逻辑;不直接 fetch;不写引导文案。
